import React from 'react';

const orderIdentifier = props => {
  if (transformedIngredients.length === 0) {
    transformedIngredients = <p>Start Building Your Dream Burger!</p>;
  }
};

export default orderIdentifier;
